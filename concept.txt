のようなコードで、SwitchbotのCO2計からのデータを取得しています。これを拡張して、以下のようなダッシュボードシステムを作りたいです。
----
[Python側]
5分毎に処理をする
取得した値を、GASにPOSTする
　日時、温度、湿度、CO2値
　GASの送り先URLは、.env(_.env)で管理する

[GAS側]
GASが値を受け取り、rawdataシートの末尾にデータを追加していく
htmlページで、データの推移を見れるようにする
　plotlyを用いる
　処理：データの範囲を指定して、GAS側(rawdataシート)からデータを取得する
　「30分前」「1時間前」「3時間前」「6時間前」「12時間前」「1日前」「3日前」「1週間前」「1ヶ月前」「1年前」のような形の指定で、取得範囲を決められるようにする
　　プルダウンがいいかな。デフォは1時間前。
　設定で、更新頻度(デフォ：3分)、より柔軟なデータ範囲設定(datetimeで範囲入力し、その範囲のデータを表示)、ができるようにする

---
[実装にあたり]
- .gsファイル、htmlファイルも、このディレクトリの中に実装する
- 処理のまとまりごとにコメントをつけたり、関数にはJSDoc, docstring(Googleスタイル)を、日本語で適切につける
----
…このようなダッシュボードシステムを実現したいです。実装計画を立ててください。
